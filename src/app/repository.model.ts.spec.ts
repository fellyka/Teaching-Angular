import { RepositoryModelTs } from './repository.model.ts';

describe('RepositoryModelTs', () => {
  it('should create an instance', () => {
    expect(new RepositoryModelTs()).toBeTruthy();
  });
});
