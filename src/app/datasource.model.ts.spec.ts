import { DatasourceModelTs } from './datasource.model.ts';

describe('DatasourceModelTs', () => {
  it('should create an instance', () => {
    expect(new DatasourceModelTs()).toBeTruthy();
  });
});
